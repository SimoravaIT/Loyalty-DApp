import React from 'react';
import styled from 'styled-components';

const Staking = () => {
	return <StyledStaking>Staking time</StyledStaking>;
};

const StyledStaking = styled.div``;

export default Staking;
